<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:resource="clr-namespace:Team32App.Resources"
             mc:Ignorable="d"
             x:Class="Team32App.Views.LanguageSelector">
    <ContentPage.Content>
        <StackLayout Margin="30,30" >
            <Label FontSize="Title" Margin="0,20" Text="{x:Static resource:AppResources.ChangeLanguage}" />
            <Picker  x:Name="picker" SelectedItem="0">
                <Picker.Items>
                    <x:String >English</x:String>
                    <x:String>French</x:String>
                    <x:String>Arabic</x:String>
                </Picker.Items>
            </Picker>
            <Button  Text="{x:Static resource:AppResources.Save}"
                       Clicked="OnUpdateLangugeClicked"
                       BackgroundColor="Black"
                       TextColor="White"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>